query GetProducts {
  products {
    nodes {
      slug
      title
      productMain {
        price
        discountPrice
        notAvailable
        productType {
          slug
          name
        }
      }
    }
  }
}
